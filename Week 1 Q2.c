#include <stdio.h>
int main (){
	float celsius ;
	float fahrenheit ;
	fahrenheit = ((celsius*9)/5 +32);
	printf ("Enter the value in celsius ");
	scanf  ("%f",&celsius);
	printf ("the value of celsius converted to fahrenheit is %f",fahrenheit =((celsius*9)/5 +32));
	return 0;
}
